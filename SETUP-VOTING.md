# 학교 소개 쇼츠 투표 시스템 — 사용 가이드

탐라교육관 체육관에서 각 학교 소개 쇼츠를 보고, 참가자(예상 64명)가
**가장 잘 만든 팀을 최대 3곳까지** 투표합니다. 결과(현황·순위)는 **진행자(관리자)만** 봅니다.

- 투표 페이지: `/vote.html` — QR로 열고 누구나 투표 (로그인 없음)
- 관리자 현황: `/vote-admin.html` — 로그인해야 실시간 순위/집계 열람

---

## 준비 (딱 한 번, 약 3분)

이 사이트는 이미 Supabase 백엔드를 쓰고 있습니다 (`SETUP-SUPABASE.md` 참고).
투표용 테이블만 한 번 추가하면 됩니다.

### 1) 투표 테이블 만들기
1. Supabase 대시보드 → **SQL Editor → New query**
2. `.moai/setup/shorts-voting-schema.sql` 파일 **전체**를 복사해 붙여넣기
3. **Run** → `Success. No rows returned` 이 나오면 완료

### 2) 관리자 계정 확인
`vote-admin.html` 로그인은 기존 관리자 계정을 그대로 씁니다.
계정이 없다면 Supabase → **Authentication → Users → Add user** 로 만드세요
(`SETUP-SUPABASE.md` 3번 참고).

### 3) 키 연결 확인
`assets/js/supabase-config.js` 가 있으면 됩니다 (기존 사이트와 동일).
없다면 `SETUP-SUPABASE.md` 4번을 따라 만드세요.
> 투표 페이지에 “투표 서버 설정이 연결되지 않았습니다” 경고가 뜨면 이 파일이 없는 것입니다.

---

## 행사 당일 사용법

1. 노트북/휴대폰으로 **`/vote-admin.html`** 열고 로그인
2. **투표 QR 코드** 카드에서 **QR 인쇄** 또는 강당 화면에 QR 띄우기
3. 참가자들이 휴대폰 카메라로 QR 스캔 → 투표
   - 1단계: 본인 소속 학교 선택 (그 학교는 투표 대상에서 자동 제외)
   - 2단계: 잘 만든 팀 **최대 3곳** 선택
   - 3단계: 확인 후 제출
4. 관리자 화면에서 **순위·득표수·참여 현황이 실시간으로 갱신**됩니다
5. 심사에 반영할 때는 **CSV 내보내기**로 최종 결과 저장

---

## 규칙 요약

| 항목 | 설정 |
|------|------|
| 선택 개수 | 1~3곳 (학생이 1곳만 고르기 어려운 점 반영) |
| 점수 | 선택 1회 = 1점 (동일 가중치), 득표순 정렬 |
| 자기 학교 | 소속 학교는 자동 제외 |
| 중복 방지 | 한 기기당 1표 (localStorage + DB UNIQUE) |
| 결과 열람 | 로그인한 관리자만 (RLS로 익명 조회 차단) |
| 대상 학교 | 25개교 / 예상 인원 64명 |

---

## 자주 묻는 질문

**Q. 한 사람이 두 번 투표할 수 있나요?**
같은 휴대폰(브라우저)에서는 다시 투표할 수 없습니다. 다른 기기를 쓰면 물리적으로 막기는
어렵지만, 현장 감독으로 충분히 통제됩니다. 더 엄격히 하려면 기기별이 아닌 1인 1코드
방식으로 확장할 수 있습니다(진행자 요청 시).

**Q. 투표자가 결과를 볼 수 있나요?**
아니요. RLS 정책상 익명 사용자는 결과를 **조회할 수 없습니다**. 오직 로그인한 관리자만
봅니다.

**Q. 학교 명단/인원을 바꾸려면?**
`assets/js/shorts-schools.js` 의 `SCHOOLS` 배열만 수정하면 투표 페이지와 관리자
화면에 동시에 반영됩니다.

**Q. 다음 행사에 다시 쓰려면?**
관리자 화면의 **전체 초기화** 버튼을 누르거나, SQL Editor 에서
`truncate table public.shorts_votes;` 를 실행하세요.

---

## 관련 파일

| 파일 | 역할 |
|------|------|
| `vote.html` | 참가자 투표 페이지 |
| `vote-admin.html` | 관리자 실시간 현황 + QR + CSV |
| `assets/js/shorts-schools.js` | 학교 명단 (단일 소스) |
| `assets/js/shorts-voting-client.js` | 투표 제출·집계 로직 |
| `.moai/setup/shorts-voting-schema.sql` | Supabase 테이블·RLS |
