# nedabah.org 빌드 의존성 (Python 3.9)
#
# 정적 사이트라 의존성 최소화 — stdlib 우선.
# 다음은 선택적 강화 자리 (없어도 기본 빌드는 동작):

# 템플릿 엔진 (선택, render_all.py 일부에서 사용)
Jinja2>=3.1.0

# Markdown front-matter 파싱 (선택, 옛 자료 호환)
python-frontmatter>=1.0.0

# RSS·Atom 피드 자동 생성 (선택, 매거진 RSS 도입 시)
# feedgen>=0.9.0

# 한국어 폰트 subset 도구 (선택, Pretendard 최적화 시)
# fonttools>=4.40.0
# brotli>=1.0.9

# CI에서만 사용
# (lhci·htmlhint·lychee·gitleaks는 npm/brew로 별도 설치)
