{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd app && pnpm install --no-frozen-lockfile && pnpm build",
  "outputDirectory": "app/.next",
  "installCommand": "cd app && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "ignoreCommand": "git diff HEAD~1 -- app/ | grep -q . || exit 1"
}
