{
  "name": "classic-fm-app",
  "version": "1.0.0",
  "private": true,
  "description": "KBS 클래식FM 지역별 주파수 모바일 앱 (PWA + Capacitor)",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios",
    "android": "cap open android",
    "ios:add": "cap add ios && cap sync ios",
    "android:add": "cap add android && cap sync android",
    "ios:build": "cap sync ios && cap open ios",
    "android:build": "cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
