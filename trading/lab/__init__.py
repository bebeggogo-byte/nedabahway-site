"""Quant Lab — multi-agent orchestration framework.

PR #2: deterministic backbone (no LLM). Wraps existing trading/src modules
into agents that communicate via Pydantic messages and a SQLite event bus.
"""
