{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/.mcp.schema.json",
  "mcpServers": {
    "context7": {
      "$comment": "Up-to-date documentation and code examples via Context7",
      "command": "/bin/bash",
      "args": ["-l", "-c", "exec npx -y @upstash/context7-mcp@latest"]
    },
    "sequential-thinking": {
      "$comment": "Step-by-step reasoning for complex problems",
      "command": "/bin/bash",
      "args": ["-l", "-c", "exec npx -y @modelcontextprotocol/server-sequential-thinking"]
    },
    "moai-lsp": {
      "$comment": "LSP code intelligence via moai binary (goto definition, find references, hover, diagnostics, rename)",
      "command": "moai",
      "args": ["mcp", "lsp"],
      "timeout": 30
    },
    "master-of-hwp": {
      "$comment": "Korean HWP/HWPX document manipulation via master-of-hwp-studio FastMCP server. Prerequisite: pip install master-of-hwp-studio (https://github.com/reallygood83/master-of-hwp)",
      "command": "mohwp",
      "args": ["mcp-serve"],
      "timeout": 30
    }
  },
  "staggeredStartup": {
    "enabled": true,
    "delayMs": 500,
    "connectionTimeout": 15000
  }
}
